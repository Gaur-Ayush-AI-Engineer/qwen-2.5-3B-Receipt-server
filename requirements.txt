# Core server
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
pydantic>=2.6.0

# MLX inference — Apple Silicon only
# On non-Mac / CI systems, this will not install; the server falls back
# to MockModelWrapper automatically.
mlx-lm>=0.13.0

# Load testing
locust>=2.24.0
